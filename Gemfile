source 'https://rubygems.org'
gem 'jekyll', '~> 4.2'

gem 'jekyll-serve'    
gem 'jekyll-watch'

# Optional Jekyll plugins (if allowed by GitHub Pages)
# gem 'jekyll-feed'    # Generates an Atom feed for your site
# gem 'jekyll-sitemap' # Generates a sitemap for your site
